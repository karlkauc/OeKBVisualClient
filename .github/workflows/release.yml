name: Build Windows Installer

on:
  release:
    types: [created, published]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-windows:
    name: Build Windows Installer
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-package: 'jdk+fx'
          java-version: '25'
          distribution: 'liberica'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        shell: bash

      - name: Build with Gradle
        run: ./gradlew build
        shell: bash

      - name: Prepare Dependencies
        run: ./gradlew prepareDependencies
        shell: bash

      - name: Create Windows Installer with jpackage
        run: ./gradlew jpackage
        shell: bash

      - name: Create Portable ZIP
        run: ./gradlew createPortableZip
        shell: bash

      - name: Find installer files
        id: find-installer
        run: |
          $msiFile = Get-ChildItem -Path build\jpackage -Filter *.msi -Recurse | Select-Object -First 1
          if ($msiFile) {
            echo "MSI_PATH=$($msiFile.FullName)" >> $env:GITHUB_OUTPUT
            echo "MSI_NAME=$($msiFile.Name)" >> $env:GITHUB_OUTPUT
          }
          $exeFile = Get-ChildItem -Path build\jpackage -Filter *.exe -Recurse | Select-Object -First 1
          if ($exeFile) {
            echo "EXE_PATH=$($exeFile.FullName)" >> $env:GITHUB_OUTPUT
            echo "EXE_NAME=$($exeFile.Name)" >> $env:GITHUB_OUTPUT
          }
        shell: pwsh

      - name: Upload MSI Installer to Release
        if: steps.find-installer.outputs.MSI_PATH
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ${{ steps.find-installer.outputs.MSI_PATH }}
          asset_name: ${{ steps.find-installer.outputs.MSI_NAME }}
          asset_content_type: application/x-msi

      - name: Upload EXE Installer to Release
        if: steps.find-installer.outputs.EXE_PATH
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ${{ steps.find-installer.outputs.EXE_PATH }}
          asset_name: ${{ steps.find-installer.outputs.EXE_NAME }}
          asset_content_type: application/x-msdownload

      - name: Find ZIP file
        id: find-zip
        run: |
          $zipFile = Get-ChildItem -Path build\jpackage -Filter *-windows-x64.zip | Select-Object -First 1
          if ($zipFile) {
            echo "ZIP_PATH=$($zipFile.FullName)" >> $env:GITHUB_OUTPUT
            echo "ZIP_NAME=$($zipFile.Name)" >> $env:GITHUB_OUTPUT
          }
        shell: pwsh

      - name: Upload ZIP (Portable App Image) to Release
        if: steps.find-zip.outputs.ZIP_PATH
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ${{ steps.find-zip.outputs.ZIP_PATH }}
          asset_name: ${{ steps.find-zip.outputs.ZIP_NAME }}
          asset_content_type: application/zip

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-packages
          path: |
            build/jpackage/*.msi
            build/jpackage/*.exe
            build/jpackage/*.zip
          retention-days: 5
